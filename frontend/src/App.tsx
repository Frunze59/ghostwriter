// App.tsx — root component
// Phase 1: just a shell to verify the dev server runs.
// Real UI comes in Phase 7.

function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="text-center">
        <h1 className="text-4xl font-bold text-gray-900 mb-2">Ghostwriter</h1>
        <p className="text-gray-500">Content generation platform — coming soon.</p>
      </div>
    </div>
  );
}

export default App;
